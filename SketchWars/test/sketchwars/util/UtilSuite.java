package sketchwars.util;

import org.junit.runner.RunWith;
import org.junit.runners.Suite;

@RunWith(Suite.class)
@Suite.SuiteClasses({
    Phys2ImageTest.class
})

public class UtilSuite {
  // the class remains empty,
  // used only as a holder for the above annotations
}